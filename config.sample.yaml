# bits Configuration
# Copy to ~/.config/bits/config.yaml (Linux) or ~/Library/Application Support/bits-cli/config.yaml (macOS)

# Active provider (coingecko, binance, bitget)
provider: coingecko

# CoinGecko configuration
coingecko:
  api_key: ""
  tier: demo  # demo or paid
  base_url: ""  # optional custom endpoint

# Binance configuration (shared API key for all markets)
binance:
  api_key: ""
  api_secret: ""
  base_url: ""  # optional custom endpoint

  # Market-specific settings
  spot:
    enabled: true  # default if nothing specified
  margin:
    enabled: false
  futures:
    enabled: false
    use_testnet: false

# Bitget configuration (shared API key for all markets)
bitget:
  api_key: ""
  api_secret: ""
  passphrase: ""
  base_url: ""  # optional custom endpoint

  # Market-specific settings
  spot:
    enabled: true  # default if nothing specified
  futures:
    enabled: false

# Symbol resolution cache settings
symbol:
  cache_ttl: "5m"
  # cache_dir: ""  # defaults to system temp directory
